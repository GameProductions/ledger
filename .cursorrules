# GameProductions Global Laws & Standards

This document serves as the absolute source of truth for all projects within the GameProductions organization. All AI interactions and development must adhere to these standards.

## 1. Cloudflare-First Infrastructure
- **Stack**: Projects must use Cloudflare's native serverless stack: Workers, Pages, D1 (SQL), KV (Key-Value), R2 (Storage), Durable Objects (State), and Queues (Background tasks).
- **Product Alignment**: All features must use the specific Cloudflare product that supports it (e.g., D1 for SQL, KV for caching).
- **Apps as PWA**: All applications are set up as Progressive Web Apps (PWA) by default.
- **Configuration**: Use `wrangler.jsonc` exclusively. Never use `wrangler.toml`.
- **Infrastructure as Code**: All bindings and environment variables must be defined in `wrangler.jsonc`.
- **Observability (v1.3.0)**: Every `wrangler.jsonc` MUST include the following block to enable full diagnostic logging and worker traces:
  ```jsonc
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1, // 100% sampling for full audit trails
    "traces": {
      "enabled": true
    }
  }
  ```
- **Git Metadata (v1.3.0)**: Every `wrangler.jsonc` MUST include a `GIT_REPO` variable in the `vars` block to link the project to its repository:
  ```jsonc
  "vars": {
    "GIT_REPO": "https://github.com/GameProductions/{repo-name}"
  }
  ```

## 2. Path-Based Routing Architecture
- **Domain Pattern**: Every project is assigned a sub-domain on `gpnet.dev` (e.g., `project-name.gpnet.dev`).
- **DNS Automation (v1.3.0)**: Cloudflare Workers automatically register DNS records for `gpnet.dev` subdomains when the `routes` property is defined in `wrangler.jsonc`.
  - All projects MUST define their custom domain route: `"{project-name}.gpnet.dev/*"`.
  - The `workers_dev` flag MUST be set to `false` for production.
- **Unified Hono Router**: Use a single Hono entry point to handle all traffic.
- **Path Branching**: 
  - `/api/*`: Reserved for bot logic, webhooks, and backend API.
  - `/admin/*`: Dedicated directory for project-level administrative tools.
  - `/godmode/*`: High-security portal for Super Admins.
  - `/*`: Reserved for the PWA (frontend) and static assets.
- **CORS**: Strict CORS policies allowing only the project's own domain and authorized local development origins.

## 3. Database & Persistence (SQL)
- **Standard**: Use the native Cloudflare D1 client (`c.env.DB`).
- **ORM Policy**: Drizzle ORM is allowed for complex schemas but is **not a requirement**. Prefer lightweight SQL wrappers or direct D1 calls for simple bots.
- **Migrations**: Maintain a `migrations/` directory managed via Wrangler. 
- **Schema Management**: Always import the database schema whenever changes are made to ensure the local environment and implementation plans stay up to date.

## 4. User & Identity Management
- **Account Creation**: Always generate a random hash as the primary user ID first. Then, link 3rd-party SSO (Discord, Google, etc.) to that hash.
- **Security Standard (v1.2.0)**:
  - **Hashing**: Use PBKDF2 with **100,000 iterations** for all password hashing (native Cloudflare Worker limit).
  - **Biometrics**: Native **WebAuthn (Passkeys)** must be supported as the primary login method for modern devices.
- **Role Hierarchy**: 
  - **Super Admin**: Full global control via the **God Mode** portal (`GodMode`).
  - **Admin**: Project-specific administrative access.
  - **Mod**: Content moderation and user management.
  - **User**: Standard access.
- **User Management**: Always include full CRUD for user management, accessible to Mods and above.
- **God Mode (`GodMode`)**:
  - **Component**: Create a separate, secure portal named `GodMode` for Super Admins.
  - **Pathing (v1.3.0)**: All God Mode tools MUST be located in `app/routes/godmode/` and use a dedicated `GodModeLayout`.
  - **Impersonation**: Must support "View As" mode with a persistent warning header.
  - **Audit Logging**: Every action in `GodMode` must be logged to a central audit trail.


## 5. UI/UX Standards (v1.2.0)
- **PWA Status**: All applications must be fully progressive and configured as a Progressive Web App (PWA).
- **High-Fidelity Utilities**:
    - **Scrollable Lists**: Use a standardized `.scrollable-list` class with `max-height: 65vh; overflow-y: auto; overflow-x: hidden;`.
    - **Custom Scrollbars**: Implement a sleek, custom-designed scrollbar for all scrollable containers.
- **Onboarding**: Every new project MUST include a high-fidelity **Guided Tour** component for first-time users.
- **Landing Page**: Must comply with Google’s safety rules to avoid "dangerous site" classification.
- **Floating Header**: 
  - Left-aligned: App Logo + App Name.
  - Right-aligned: User drop-down menu with avatar.
- **Universal Footer**: 
  - Mandatory on every application.
  - Content: - Top Row: App Name + Version (Left), Legal Links (Right); - Bottom Row: `[Current Year] GameProductions™. All rights reserved.`
- **Notifications**: Dismissable toasts only. Do NOT use browser alerts.

## 6. Local Development & Deployment
- **Local-First Testing**: Mandatory local testing before any deployment. 
- **Remote DB Binding**: Use `wrangler dev --remote` to connect local dev to the live D1 database.
- **Publishing Workflow**: Local Test -> User Confirmation -> Commit (with version) -> Publish.
- **Refactoring Policy**: Keep PWAs modular. If a component grows too large, suggest refactor options. **Never refactor without user confirmation.**

## 7. Development & DevOps (v1.3.0)
- **Version Control**: GitHub (`GameProductions/`) for all code management.
- **CI/CD**: Use GitHub Actions for auto-deploy.
- **Versioning**: Major (X.0.0), Minor (0.X.0), Patch (0.0.X). Prefix commits (e.g., `v1.2.3: Add feature X`).
- **CLI Maintenance**: Always keep `gh`, `wrangler`, and other CLI tools updated.
- **Security & Hardening**: Maintain absolute health for Dependabot and vulnerability monitoring. Resolve security concerns immediately.
- **Project Synchronization**: Maintain `implementation_plan.md` and sync with GitHub Project.

## 8. Project Initialization
- All new projects must be initialized via `create-project.md`.
- **Legacy Migration**: Requires **manual approval**. Immediately notify the user if a repo is not v1.3.0 standard.


## 9. Standard Directory Structure (v1.3.0)
All projects MUST adhere to this directory layout:

### Core Orchestration (Root)
- `server.ts`: The unified Hono entry point (Router/Orchestrator).
- `wrangler.jsonc`: The Cloudflare configuration source of truth.
- `database/`: Centralized SQL schemas, migrations, and seeds.
- `types/`: Global TypeScript definitions and bindings.

### Application Logic (`app/`)
- `app/routes/`: Route entry points for UI pages.
- `app/layouts/`: Page-level frames (e.g., `RootLayout`, `GodModeLayout`).
- `app/components/`: Project-specific UI components.
- `app/hooks/`: Custom React hooks.
- `app/context/`: React Context providers.
- `app/services/`: Client-side logic for external APIs, SSO, and WebAuthn.
- `app/styles/`: Global CSS and HSL design tokens.
- `app/utils/`: Shared frontend utility functions.

### Bot Logic (`apps/bot/`)
- `apps/bot/commands/`: Discord/Slack command handlers.
- `apps/bot/events/`: Discord/Slack event listeners.
- `apps/bot/middleware/`: Bot-specific security and validation.
- `apps/bot/routes/`: Hono routes for bot webhooks/APIs.
- `apps/bot/utils/`: Bot helpers (embeds, filters).

### Standardized Public Directory (`public/`)
- `public/`: Root level for `manifest.json`, `favicon.ico`, `robots.txt`.
- `public/brand/`: App Logo, App Icon, and Banners.
- `public/legal/`: `privacy.md`, `terms.md`, `security.md`.
- `public/well-known/`: Platform verification files.

### Operations
- `app/telemetry/`: Standardized audit trail and diagnostic logic.
- `scripts/`: CI/CD, migration, and maintenance utilities.
- `src/components/foundation/`: READ-ONLY organizational components.


## 10. Tone & Branding
- **God Mode** is the only allowed themed jargon.
- **Branding**: GameProductions copyright MUST be a permanent fixture in the footer.
- **Versioning**: Only permitted in the App Footer and update prompts.
- **Fluid Responsiveness**: Layouts must natively adjust based on visible space.
- **Accessibility**: User Settings must include Contrast, Font Scaling, and Reduced Motion.


